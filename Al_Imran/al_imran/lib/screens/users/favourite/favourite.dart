import 'package:flutter/material.dart';

class UserFavourite extends StatelessWidget {
  const UserFavourite({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is categor screen'),
    );
  }
}
